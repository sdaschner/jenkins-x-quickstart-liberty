buildPack: liberty
pipelineConfig:
  pipelines:
    release:
      build:
        steps:
        - sh: mvn package liberty:start-server failsafe:integration-test liberty:stop-server failsafe:verify
          name: mvn-liberty-integration-test
  agent:
    container: maven-java11
